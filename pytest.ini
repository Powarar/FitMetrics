[pytest]
# Пути
testpaths = tests
pythonpath = .

# Паттерны
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Asyncio конфигурация
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session

# Опции
addopts = 
    --strict-markers
    --tb=short
    -ra
